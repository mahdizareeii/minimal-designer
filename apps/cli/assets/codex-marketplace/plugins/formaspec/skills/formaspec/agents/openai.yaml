interface:
  display_name: "FormaSpec"
  short_description: "Design and refine products with FormaSpec"
  default_prompt: "Use $formaspec to design this interface with FormaSpec."
dependencies:
  tools:
    - type: "mcp"
      value: "formaspec"
      description: "Structured FormaSpec design, specification, preview, lint, task, and commit tools"
      transport: "streamable_http"
      url: "http://127.0.0.1:4312/mcp"
