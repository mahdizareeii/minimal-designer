interface:
  display_name: "Minimal UI"
  short_description: "Design and refine products with Minimal UI"
  default_prompt: "Use $minimal-ui to design this interface with Minimal UI."
dependencies:
  tools:
    - type: "mcp"
      value: "formaspec"
      description: "Structured FormaSpec design, specification, preview, lint, task, and commit tools"
      transport: "streamable_http"
      url: "http://127.0.0.1:4312/mcp"
